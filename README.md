# Recipe Book

Simple React app demonstrating `useState` and `useEffect`.

Run:

```bash
npm install
npm run dev
```

Features:
- Add recipes with validation
- Live search, category filter, sorting
- Like (favorite), delete, details modal
- Mock API load with "Loading..." and localStorage persistence
